# Guia de implementacion del chatbot (supervisado)

import nltk
nltk.download('punkt')
python -c "import nltk.download('punkt')

visual code
python -m pip install nltk

instalar librerias
"pip install scikit-learn nltk numpy gym==0.26.2 gym-notices"
